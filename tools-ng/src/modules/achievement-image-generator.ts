import { logger } from '../utils/logger.js';

export function generateAchievementImage(achievementId: string, outputDir: string): void {
  logger.section('Achievement Image Generator');
  logger.info(`Generating image for: ${achievementId}`);
  logger.warn('Achievement image generation coming soon');
}
