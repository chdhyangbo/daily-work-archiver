import { logger } from '../utils/logger.js';

export function generateAchievementCard(achievementId: string, outputDir: string): void {
  logger.section('Achievement Card Generator');
  logger.info(`Generating card for: ${achievementId}`);
  logger.warn('Achievement card generation coming soon');
}
